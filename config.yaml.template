# Reseller Telegram Bot Configuration Template

bot_token: "YOUR_TELEGRAM_BOT_TOKEN"
backend_url: "https://your-backend-domain.com" # URL of the Core Backend Server
reseller_api_key: "rn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
admin_chat_ids:
  - 123456789 # Replace with actual admin Telegram chat IDs

# Optional: Direct host-to-IP overrides (bot-level /etc/hosts bypass for CDNs/firewalls)
# host_mappings:
#   "your-backend-domain.com": "1.2.3.4"


# Optional: Skip TLS verification (useful when bypassing CDN or testing with self-signed certs)
# insecure_skip_verify: false

